# game export extracted for reference — 31k files, never committed
export/
# harness worktrees
.claude/.worktree/
# local-only recordings (mp4 kept, raw webm never)
recordings/*.webm
# rendered review twins are build artifacts
docs/plans/*.html
__pycache__/
*.pyc
.pytest_cache/
# .NET build output
plugin/**/bin/
plugin/**/obj/

# release staging
dist/

# raw screenshot drops; the named copies in docs/images are what the README uses
screenshots/
